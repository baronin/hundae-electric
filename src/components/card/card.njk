{% macro card(pathImg='', altText='alt', title='', desc='') %}
  <div class="card-image-holder">
    <img src="{{ pathImg }}"
         alt="{{ altText }}">
  </div>
  <div class="card-content">
    <h4>{{ title }}</h4>
    <p>{{ desc }}</p>
  </div>
{% endmacro %}
